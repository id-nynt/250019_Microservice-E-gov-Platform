services:
  apigateway:
    container_name: "apigateway"
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5000:80
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - universe-network
  

networks:
  universe-network:
    external: true
